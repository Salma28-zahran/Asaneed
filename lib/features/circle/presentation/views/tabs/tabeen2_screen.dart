import 'package:flutter/material.dart';

class Tabeen2Screen extends StatelessWidget {
  const Tabeen2Screen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
